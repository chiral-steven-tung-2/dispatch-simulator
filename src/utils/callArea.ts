/** Perimeter radius (m) every call starts at before any units arrive. */
export const START_CALL_RADIUS = 20;

/** How much the perimeter grows (m) for each unit that arrives on scene. */
export const CALL_RADIUS_STEP = 2;
